import filters from './lib/filters/filters';
import Canvas from './lib/util/canvas';

// Canvas
export { Canvas };

// Filters
export const { invert } = filters;
export const { greyscale } = filters;
export const { grayscale } = filters;
export const { invertGreyscale } = filters;
export const { invertGrayscale } = filters;
export const { sepia } = filters;
export const { silhouette } = filters;
export const { threshold } = filters;
export const { invertedThreshold } = filters;
export const { brightness } = filters;
export const { darkness } = filters;
export const { myOldFriend } = filters;
export const { sharpen } = filters;
export const { blur } = filters;
export const { convolute } = filters;
